{
  "name": "school-management-app-firestore-rules",
  "private": true,
  "scripts": {
    "test:firestore-rules": "firebase emulators:exec --only firestore --project demo-school-management \"node --test firestore/tests/firestore.rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "firebase-tools": "^14.3.1"
  }
}
